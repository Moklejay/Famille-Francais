# Local family progress and API key -- never commit these.
# (If you deploy to a shared/public repo, keeping data/ out of git is what
# stops your API key and your sister's chat history from being published.)
data/ai_config.json
data/family_data.json
data/*.json
!data/.gitkeep

__pycache__/
*.pyc
.streamlit/secrets.toml
